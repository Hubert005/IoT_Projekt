# Agents

<agent>
<name>ble-integrator</name>
<description>Keep BLE communication with the ESP32 mixer stable and compatible. Preserves the Nordic UART Service protocol, verifies message format compatibility, and validates test mode behavior without hardware.</description>
</agent>

<agent>
<name>planner</name>
<description>Plan multi-step changes for this Flutter project before implementation. Inspects files, identifies smallest safe change sets, and coordinates between UI, services, and models.</description>
</agent>

<agent>
<name>ui-reviewer</name>
<description>Review Flutter UI changes for clarity, consistency, and maintainability. Ensures layout and theme alignment with lib/core/theme/, favors reusable widgets, and preserves readability.</description>
</agent>

<agent>
<name>feature-builder</name>
<description>Scaffold and implement new features rapidly while enforcing architectural consistency. Generates folder structures, applies templates, validates architecture compliance, and ensures UI/style consistency.</description>
</agent>
